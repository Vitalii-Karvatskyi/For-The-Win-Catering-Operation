import type { CateringEvent } from '../types/cateringOperations';

/**
 * Seed data for catering operations.
 * Empty on purpose — events are created through the Add Catering form.
 */
export const caterings: CateringEvent[] = [];
